{
  "name": "bitbucket-mcp",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10.9.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "prettier --check README.md package.json tsconfig.json tsconfig.test.json vitest.config.mts src test .github/bin/pipelines/test-nodejs/README.md .github/bin/pipelines/test-nodejs/*.cjs .github/workflows/test-nodejs.yml && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "format": "prettier --write README.md package.json tsconfig.json tsconfig.test.json vitest.config.mts src test .github/bin/pipelines/test-nodejs/README.md .github/bin/pipelines/test-nodejs/*.cjs .github/workflows/test-nodejs.yml",
    "start": "node --env-file-if-exists=.env --enable-source-maps .dist/index.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/node": "22.20.3",
    "@vitest/coverage-v8": "5.0.1",
    "prettier": "3.9.8",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
